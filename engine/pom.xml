<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.graduation.project</groupId>
    <artifactId>engine</artifactId>
    <version>1.0.0</version>
    <name>engine</name>
    <description>engine</description>
    <!--
      The licence of the SOURCE in this repository, matching LICENSE and NOTICE at the root. The
      pom had no <licenses> at all, so the one artifact of this project that carries structured
      metadata to a repository claimed nothing, and a consumer resolving it saw only the parent's.

      A binary built from this source is narrower than "or later", and the difference is real
      rather than pedantic. com.itextpdf:itextpdf 5.5.13.x - see the itext.pdf.version property
      below - is AGPL version 3 only: its own pom names "GNU Affero General Public License v3"
      and stops there, with no "or (at your option) any later version". Its source headers add,
      under section 7(b), that "a covered work must retain the producer line in every PDF that is
      created or manipulated using iText". Both were read out of the artifact in the local
      repository rather than recalled.

      So conveying the built jar is bound to AGPL-3.0 specifically, and the producer line has to
      survive in the PDFs it generates. EventService writes them from a bare com.itextpdf Document
      and never touches the Producer field, so that requirement is met by default today - it would
      only become a question if something started setting it. Distributing the source is
      unaffected; this project's own grant is the "or later" one declared below.
    -->
    <licenses>
        <license>
            <name>AGPL-3.0-or-later</name>
            <url>https://www.gnu.org/licenses/agpl-3.0.html</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <properties>
        <java.version>17</java.version>
        <!--
          0.13.0. The 0.12 line removed Jwts.parserBuilder() outright - Jwts.parser() now returns
          the JwtParserBuilder that parserBuilder() used to - so this is a source-incompatible
          bump, not a drop-in one. See JwtService for the calls that moved.
        -->
        <jjwt.version>0.13.0</jjwt.version>
        <!--
          springdoc is NOT in spring-boot-dependencies, so this pin is real work rather than a
          restatement of the parent: it has to be moved by hand whenever the parent moves.
          2.9.0 is the last 2.x and its own parent pom is spring-boot-starter-parent 3.5.16 -
          an exact match for ours. springdoc 3.x targets Boot 4 and must not be used here.
        -->
        <swagger.openapi.version>2.9.0</swagger.openapi.version>
        <itext.pdf.version>5.5.13.3</itext.pdf.version>
        <jacoco.maven.plugin.version>0.8.15</jacoco.maven.plugin.version>
        <testcontainers.version>1.21.3</testcontainers.version>
        <!--
          Placeholder for JaCoCo's late-replaced @{argLine}. Surefire is configured with
          <argLine>@{argLine}</argLine>; without this default the build fails whenever
          jacoco:prepare-agent has not run (e.g. `mvn surefire:test` on its own).
        -->
        <argLine></argLine>
        <!--
          Same guard for Failsafe. jacoco:prepare-agent-integration sets `failsafeArgLine`;
          without this default `mvn failsafe:integration-test` on its own would fail to
          interpolate @{failsafeArgLine}.
        -->
        <failsafeArgLine></failsafeArgLine>
    </properties>
    <dependencyManagement>
        <dependencies>
            <!--
              Aligns testcontainers-core / -mongodb / -kafka / -junit-jupiter on one version.
              Deliberately NOT tied to spring-kafka: the whole point of preferring
              Testcontainers over @EmbeddedKafka here is that the broker used by the tests
              does not move when spring-kafka moves (3.0.x = ZooKeeper broker, 3.1+ = KRaft).
            -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
        </dependency>

        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${swagger.openapi.version}</version>
        </dependency>

        <dependency>
            <groupId>com.itextpdf</groupId>
            <artifactId>itextpdf</artifactId>
            <version>${itext.pdf.version}</version>
        </dependency>

        <!-- Testcontainers: only *IT classes (Failsafe) touch these; `mvn test` never does. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mongodb</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
          Declared here, and still with no <version>: spring-boot-dependencies manages it
          (4.2.2 on Boot 3.5.16).

          This entry used to be the ONLY way to get awaitility. On Boot 3.0.4
          spring-boot-starter-test pulled in JUnit, Mockito, AssertJ, Hamcrest, JSONassert,
          JsonPath and XMLUnit, and awaitility was *managed* but never declared - verified in
          both starters when the parent moved. Boot 3.5.16's starter declares it, so it now
          also arrives transitively and this declaration is no longer load-bearing. It stays
          because a dependency the ITs name for themselves cannot quietly vanish the next time
          the starter's contents are rearranged.

          Needed because the Kafka ITs assert on a side effect produced asynchronously on a
          consumer thread; a Thread.sleep long enough to be reliable is also long enough to
          dominate the IT runtime, and one short enough to be fast is flaky.
        -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- @{...} is Surefire late replacement: resolved after jacoco:prepare-agent. -->
                    <argLine>@{argLine}</argLine>
                    <systemPropertyVariables>
                        <spring.profiles.active>test</spring.profiles.active>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <!--
              Failsafe owns the *IT classes. The split is the point:

                ./mvnw test      -> Surefire only. No Docker, no containers, seconds.
                                    This is the TDD inner loop and must stay that way.
                ./mvnw verify    -> Surefire, then Failsafe (Docker required).

              No <executions> block here on purpose: spring-boot-starter-parent already
              declares integration-test + verify in its <pluginManagement>, and re-declaring
              them risks binding the goals twice. It also sets
              <classesDirectory>${project.build.outputDirectory}</classesDirectory> so the ITs
              run against target/classes rather than the repackaged fat jar.

              forkCount=1 + reuseForks=true keeps every IT class in ONE JVM, which is what
              makes AbstractIntegrationTest's *static* containers a genuine singleton: the
              containers start once and the whole IT suite shares them. Forking per class
              would silently pay full container startup for every IT.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- Late replacement, resolved after jacoco:prepare-agent-integration. -->
                    <argLine>@{failsafeArgLine}</argLine>
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <systemPropertyVariables>
                        <spring.profiles.active>test</spring.profiles.active>
                        <!--
                          Containers start before any Spring context, so Logback is still
                          unconfigured (root=DEBUG) and docker-java dumps every HTTP wire frame.
                          Scoped to this JVM only; Surefire keeps Spring Boot's defaults.
                        -->
                        <logback.configurationFile>logback-it.xml</logback.configurationFile>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <!--
              JaCoCo is split so integration coverage is not invisible:

                initialize            prepare-agent             -> target/jacoco.exec     (argLine)
                package               report                    -> target/site/jacoco     (unit only)
                pre-integration-test  prepare-agent-integration -> target/jacoco-it.exec  (failsafeArgLine)
                post-integration-test merge                     -> target/jacoco-merged.exec
                post-integration-test report                    -> target/site/jacoco-merged

              merge/report are bound to post-integration-test, NOT verify: failsafe:verify runs
              at verify and fails the build on IT failures, which would skip the report exactly
              when you most want to read it.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.maven.plugin.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-initialize</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-site</id>
                        <phase>package</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-initialize-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco-it.exec</destFile>
                            <propertyName>failsafeArgLine</propertyName>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jacoco-merge</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <!-- Explicit, so a stale jacoco-merged.exec is never re-merged into itself. -->
                                    <includes>
                                        <include>jacoco.exec</include>
                                        <include>jacoco-it.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jacoco-site-merged</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <outputDirectory>${project.reporting.outputDirectory}/jacoco-merged</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
